{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "cd ./upload && npm run build",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^6.6.1"
  },
  "dependencies": {
    "vite": "^4.2.2",
    "vue": "^3.2.47"
  }
}
